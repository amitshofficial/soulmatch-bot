# SoulMatch Bot

Telegram dating bot prototype.

## Deploy
Set `BOT_TOKEN` as an environment variable (Railway Variables), then deploy.

## Notes
- Uses sqlite by default (dating_bot.db).
- For production use Postgres or Railway Postgres addon.
